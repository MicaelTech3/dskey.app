<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dsigner.dskey">

    <!-- SUPORTE ANDROID TV -->
    <uses-feature
        android:name="android.software.leanback"
        android:required="false" />

    <!-- TOUCHSCREEN OPCIONAL -->
    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO"/>
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>

    <application
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:banner="@drawable/banner"
        android:theme="@style/Theme.DsKey"
        android:usesCleartextTraffic="true">

        <!--
            🚪 ENTRY POINT — WelcomeActivity é a porta de entrada.
            Na primeira abertura mostra onboarding. Nas seguintes vai direto para MainActivity.
            Ela herda todos os intent-filters de launcher pois é quem o sistema chama.
        -->
        <activity
            android:name=".WelcomeActivity"
            android:exported="true"
            android:screenOrientation="landscape">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <!-- 🔥 ESSENCIAL PARA LAUNCHER (abre com o sistema) -->
                <category android:name="android.intent.category.HOME" />
                <category android:name="android.intent.category.DEFAULT" />

                <!-- ANDROID TV -->
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />

                <!-- MOBILE (opcional) -->
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <!-- MainActivity não precisa de intent-filter, é chamada pela WelcomeActivity -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:screenOrientation="landscape" />

    </application>

</manifest>