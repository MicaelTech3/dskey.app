<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#000000">

    <!-- ================= VÍDEO FULLSCREEN ================= -->
    <androidx.media3.ui.PlayerView
        android:id="@+id/playerView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="gone"
        android:keepScreenOn="true"
        android:background="#000000" />

    <!-- ================= IMAGEM FULLSCREEN ================= -->
    <ImageView
        android:id="@+id/imageView"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop"
        android:visibility="gone"
        android:background="#000000" />

    <!-- ================= OVERLAY DO CÓDIGO (TOQUE / VOLTAR) ================= -->
    <LinearLayout
        android:id="@+id/codeOverlay"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:orientation="vertical"
        android:gravity="center"
        android:padding="24dp"
        android:background="#CC000000"
        android:visibility="gone">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="CÓDIGO"
            android:textColor="#00FFCC"
            android:textSize="20sp"
            android:textStyle="bold"/>

        <TextView
            android:id="@+id/tvOverlayCode"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="ABC-123"
            android:textColor="#FFFFFF"
            android:textSize="18sp"
            android:fontFamily="monospace"
            android:paddingTop="12dp"/>
    </LinearLayout>

    <!-- ================= TELA INICIAL / SEM MÍDIA ================= -->
    <LinearLayout
        android:id="@+id/generatorLayout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center"
        android:background="#0e132a">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="D'SIGNER"
            android:textSize="34sp"
            android:textStyle="bold"
            android:textColor="#00d4ff"/>

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="VÁ PARA NOSSO PAINEL EM\ndsignertv.com.br"
            android:textColor="#9adfff"
            android:textSize="16sp"
            android:gravity="center"
            android:padding="12dp"/>

        <TextView
            android:id="@+id/tvKey"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="ABC-123"
            android:textColor="#00FFCC"
            android:textSize="36sp"
            android:textStyle="bold"
            android:fontFamily="monospace"
            android:padding="24dp"
            android:background="@drawable/bg_code_neon"/>
    </LinearLayout>

</FrameLayout>
